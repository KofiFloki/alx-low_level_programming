#include <studio.h>

/**
 * main - prints out a string via printf
 * Description: Program should print out the string followed by a line
 * Return: int 0 when done with no errors
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
